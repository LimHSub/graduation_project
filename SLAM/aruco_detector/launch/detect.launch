<launch>
  <!-- 카메라 드라이버 -->
  <node name="usb_cam" pkg="usb_cam" type="usb_cam_node" output="screen">
    <param name="video_device" value="/dev/video2"/>
    <param name="pixel_format" value="mjpeg"/>
    <param name="image_width"  value="1280"/>
    <param name="image_height" value="720"/>
    <param name="framerate"    value="30"/>
    <param name="camera_name" value="usb_cam"/>
    <param name="camera_frame_id" value="usb_cam"/>
    <!-- 필요하면 calibration yaml 경로 지정 -->
    <param name="camera_info_url" value="file:///home/inwoong/catkin_ws/src/aruco_detector/config/camera.yaml"/>
  </node>

  <!-- image_proc는 카메라 네임스페이스에서 -->
  <group ns="usb_cam">
    <node pkg="image_proc" type="image_proc" name="image_proc"/>
  </group>

  <!-- 아루코 노드: 상대 토픽(image, camera_info)을 아래처럼 remap -->
  <node name="aruco_detector" pkg="aruco_detector" type="aruco_detector_node" output="screen">
  <remap from="~image"       to="/usb_cam/image_raw"/>
  <remap from="~camera_info" to="/usb_cam/camera_info"/>
    
      <!-- 카메라가 정면/수평이면 아래 두 값은 기본으로 충분 -->
  <param name="use_optical_to_base" value="true"/>
  </node>
</launch>

